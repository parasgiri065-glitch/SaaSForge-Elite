/**
 * localStorage key shared by the inline boot script and the theme provider.
 * Changing this value would flash the wrong scheme for existing visitors.
 */
export const THEME_STORAGE_KEY = "saasforge-theme";
